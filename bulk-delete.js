(function(){function setup(){const container=document.getElementById('pdfContainer');if(!container||document.getElementById('bulkDeleteBar'))return;const bar=document.createElement('div');bar.id='bulkDeleteBar';bar.style.cssText='display:none;align-items:center;gap:12px;margin:0 0 15px;padding:12px 16px;background:#fff;border-radius:14px;box-shadow:0 4px 14px rgba(0,0,0,.06);font-weight:700;';bar.innerHTML='<input id="selectAllFiles" type="checkbox" style="width:18px;height:18px;cursor:pointer"><span id="selectedFilesCount">0 selezionati</span><button id="bulkDeleteBtn" type="button" style="margin-left:auto;border:0;border-radius:10px;padding:10px 16px;background:#cc0000;color:#fff;font-weight:700;cursor:pointer"><i class="fa-solid fa-trash-can"></i> Elimina selezionati</button>';container.parentNode.insertBefore(bar,container);const count=()=>{const boxes=[...container.querySelectorAll('.bulk-file-checkbox')],selected=boxes.filter(x=>x.checked);document.getElementById('selectedFilesCount').textContent=selected.length+' selezionati';bar.style.display=selected.length?'flex':'none';document.getElementById('selectAllFiles').checked=boxes.length>0&&selected.length===boxes.length;};container.addEventListener('change',e=>{if(e.target.classList.contains('bulk-file-checkbox'))count()});document.getElementById('selectAllFiles').addEventListener('change',e=>{container.querySelectorAll('.bulk-file-checkbox').forEach(x=>x.checked=e.target.checked);count()});document.getElementById('bulkDeleteBtn').addEventListener('click',async()=>{const selected=[...container.querySelectorAll('.bulk-file-checkbox:checked')].map(x=>({name:x.dataset.fileName}));if(!selected.length)return;if(!confirm(`Vuoi eliminare definitivamente ${selected.length} file selezionati?`))return;let token='';try{token=GITHUB_TOKEN}catch(e){}let owner='laminetallth',repo='TOP-HOUSE',folder='';try{owner=OWNER;repo=REPO;folder=FOLDER_PATH}catch(e){}if(!token){alert('Token GitHub non disponibile.');return}const btn=document.getElementById('bulkDeleteBtn');btn.disabled=true;btn.textContent='Eliminazione in corso...';let errors=0;try{const listing=await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/${folder}`,{headers:{Authorization:`token ${token}`}}),files=listing.ok?await listing.json():[],byName=new Map((Array.isArray(files)?files:[]).map(f=>[f.name,f]));for(const file of selected){const current=byName.get(file.name);if(!current?.sha){errors++;continue}try{const r=await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/${folder}/${encodeURIComponent(file.name)}`,{method:'DELETE',headers:{Authorization:`token ${token}`,'Content-Type':'application/json'},body:JSON.stringify({message:`Eliminato file: ${file.name}`,sha:current.sha})});if(!r.ok)errors++}catch(e){errors++}}}catch(e){errors=selected.length}alert(errors?`Operazione completata con ${errors} errore/i.`:'File eliminati con successo.');if(typeof window.loadFiles==='function')window.loadFiles();else location.reload()})}function decorate(){const container=document.getElementById('pdfContainer');if(!container)return;container.querySelectorAll('.file-item-wrapper').forEach(row=>{if(row.querySelector('.bulk-file-checkbox'))return;const link=row.querySelector('.file-link'),del=row.querySelector('.delete-btn');if(!link||!del)return;const name=link.querySelector('.file-name')?.textContent?.trim()||'',checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.className='bulk-file-checkbox';checkbox.dataset.fileName=name;checkbox.style.cssText='width:18px;height:18px;cursor:pointer;flex:0 0 auto;margin-right:12px;';checkbox.addEventListener('click',e=>e.stopPropagation());link.insertBefore(checkbox,link.firstChild)})}setup();new MutationObserver(decorate).observe(document.getElementById('pdfContainer')||document.body,{childList:true,subtree:true});setInterval(decorate,500)})();